# React Native's Metro bundler cannot follow pnpm's strict symlinked layout —
# packages like expo-modules-core end up unreachable from their dependents.
# A hoisted node_modules is the documented arrangement for RN monorepos.
node-linker=hoisted
