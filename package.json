{
  "name": "eighteen-zero",
  "private": true,
  "version": "0.1.0",
  "description": "18-0 \u2014 Spin history. Build seven. Chase perfection.",
  "scripts": {
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck",
    "sim": "pnpm --filter @18-0/domain sim",
    "calibrate": "pnpm --filter @18-0/domain calibrate"
  },
  "packageManager": "pnpm@10.27.0",
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild"
    ]
  }
}